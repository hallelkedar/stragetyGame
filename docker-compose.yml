services:
  server:
    container_name: armyServer
    build:
      context: ./
      dockerfile: ./Dockerfile
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - PORT=3001
    ports:
      - "3000:3001"
